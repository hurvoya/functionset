# Remplacer les virgules par des points, enlever les espaces et convertir en float
df['MT_NEW_BUSINESS'] = (
    df['MT_NEW_BUSINESS']
    .str.replace(',', '.', regex=False)
    .str.replace(' ', '', regex=False)
    .astype(float)
)
