df_created["email sender type"] = np.where(
    df_created["email sender"].isna(),
    pd.NA,
    np.where(
        df_created["email sender"].str.lower().str.contains("@bnpp", na=False),
        "internal",
        "external"
    )
)
