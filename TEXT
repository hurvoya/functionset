import numpy as np

df_created["flag"] = np.where(
    (~df_created["incoming channel"].isin(["DUP", "CSV"])) &
    (~df_created["email sender"].str.contains("reply", case=False, na=False)),
    1,
    0
)
