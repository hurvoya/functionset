robot_ids = df_robot["id_robot"].dropna().astype(str).str.strip()
robot_ids = set(robot_ids)

for df in [df_created, df_stock, df_closed]:
    df["flag_robot"] = (
        df["case manager"]
        .astype(str)
        .str.strip()
        .isin(robot_ids)
        .astype(int)
    )
