def aggregate_user_stats(dfcc):
    """Aggregate customer statistics for Power BI reporting.

    The function builds an aggregated table from the prepared customer dataset
    to reduce the volume of data loaded into Power BI. It computes customer
    statistics across the variables defined in ``group_vars``, including the
    total number of unique customers and the number of registered customers
    based on the sum of the customer account creation flag.

    Parameters
    ----------
    dfcc : pandas.DataFrame
        Prepared customer dataframe containing customer identifiers, the
        customer account creation flag, and the variables used for aggregation.

    Returns
    -------
    pandas.DataFrame
        Aggregated customer statistics dataframe with one row per combination
        of grouping variables, including the total number of unique customers
        and the number of registered customers.
    """
