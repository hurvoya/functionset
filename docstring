def merge_and_clean_user(dfcc, df_extractcc):
    """Merge contract data with customer account connection information.

    The function enriches the contract/customer dataframe with customer account
    and connection information from an external customer account extract. It
    also performs basic cleaning and preparation steps, including extracting
    year and month values, replacing selected values, creating a customer
    account existence flag, and selecting the final set of columns.

    Parameters
    ----------
    dfcc : pandas.DataFrame
        Contract/customer dataframe computed from database sources.

    df_extractcc : pandas.DataFrame
        Customer account extract containing information related to customer
        portal accounts and connection activity.

    Returns
    -------
    pandas.DataFrame
        Final enriched dataframe combining contract/customer information with
        customer account and connection data, including the customer account
        existence flag and the selected final columns.
    """
