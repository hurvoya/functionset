def clean_nan_df(df):
    """Clean missing values in a dataframe.

    The function replaces actual pandas missing values with ``"Not defined"``.
    It also replaces string values that are exactly equal to ``"nan"`` or
    ``"(nan)"`` with ``"Not defined"``, while leaving other strings containing
    ``"nan"`` as part of a larger word unchanged.

    Parameters
    ----------
    df : pandas.DataFrame
        Dataframe to clean.

    Returns
    -------
    pandas.DataFrame
        Cleaned dataframe with missing values standardized as ``"Not defined"``.
    """
