def prepare_user_calls(dfCC):
    """Prepare the user calls dataframe for downstream processing.

    The function standardizes the input calls data by enforcing data types,
    renaming columns, creating combined label/code columns, and selecting the
    final set of columns.

    Parameters
    ----------
    dfCC : pandas.DataFrame
        Input dataframe containing raw user call data.

    Returns
    -------
    pandas.DataFrame
        Prepared user calls dataframe with standardized data types, renamed
        columns, combined label/code fields, and the selected final columns.
    """
