for year in [2024, 2025]:
    # Global
    plot_scatter_calls_vs_montant(dealer_activity, year=year, ibl_scope="GLOBAL")
    # ELS
    plot_scatter_calls_vs_montant(dealer_activity, year=year, ibl_scope="ELS")
    # TLS
    plot_scatter_calls_vs_montant(dealer_activity, year=year, ibl_scope="TLS")
