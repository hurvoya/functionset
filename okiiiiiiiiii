evt_dates = events[["country", "id_apporteur", "event_id", "dte_new_dealer"]].copy()

stock = stock.merge(
    evt_dates,
    on=["country", "id_apporteur", "event_id"],
    how="left"
)


stock["dte_new_dealer"].isna().sum()
