# Exemple d'appel
code_label_pairs = [
    ('CODE_PRODUIT', 'LIB_PRODUIT'),
    ('CODE_MARCHE', 'LIB_MARCHE'),
    ('CODE_CLIENT', 'LIB_CLIENT')
]

pivot_mapping = build_pivot_mapping(df, code_label_pairs)
pivot_mapping.head()
