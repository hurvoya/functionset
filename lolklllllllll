def build_existing_events(
    metrics,
    nd_windows,
    follow_existing
):
    step1 = filter_existing_business(metrics)
    step2 = exclude_new_dealers(step1, nd_windows)
    step3 = exclude_existing_in_follow(step2, follow_existing)

    return step3
