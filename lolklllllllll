chk = calls_existing_yoy.groupby(["country","id_apporteur","dte_existing_event"]).size().describe()
print(chk)
print((calls_existing_yoy.groupby(["country","id_apporteur","dte_existing_event"]).size() > 8).sum())
