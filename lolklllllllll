# 1) volumétrie doit être raisonnable
events_prev6m.shape, events_yoy.shape

# 2) pas de multi-events mensuels pendant suivi EG (cooldown)
events_prev6m.groupby(["country","ibl","id_apporteur"]).size().describe()

# 3) cap calls à 8
follow_prev6m.groupby(["country","ibl","id_apporteur","event_id"]).size().max()
