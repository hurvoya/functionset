
---

# 🧱 Étape D1 — Filtrage business pur (500k + 30%)

```python
def filter_existing_business(metrics):
    out = metrics.copy()

    out = out[
        (out["prod_current_6m"] >= 500_000) &
        (out["growth_pct"] >= 0.30)
    ].copy()

    return out
