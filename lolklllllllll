print(stock_existing_yoy_es["sorti_suivi"].value_counts(dropna=False))
# attendu: beaucoup de False + quelques True (1 True par event)

# vérifier qu'il y a 1 seul True par event
chk = stock_existing_yoy_es.groupby(["id_apporteur","event_id"])["sorti_suivi"].sum()
print(chk.describe())
