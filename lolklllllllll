# events = events_yoy  # ou events_prev6m

kpi_global = build_kpi_block1_volume(events, ibl=None)
plot_block1_volume(kpi_global, "Existing Dealers Volume — Key Account vs Non Key Account (Global)")

kpi_els = build_kpi_block1_volume(events, ibl="ELS")
plot_block1_volume(kpi_els, "Existing Dealers Volume — Key Account vs Non Key Account (ELS)")

kpi_tls = build_kpi_block1_volume(events, ibl="TLS")
plot_block1_volume(kpi_tls, "Existing Dealers Volume — Key Account vs Non Key Account (TLS)")
