row = top.iloc[0]
c, dealer = row["country"], row["id_apporteur"]

events_anom = (
    events[(events["country"] == c) & (events["id_apporteur"] == dealer)]
    .sort_values("date_new_dealer")
)

events_anom.head(20), events_anom.tail(20), len(events_anom)
