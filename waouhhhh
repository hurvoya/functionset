summary_bloc5 = (
    kpi_bloc5
    .groupby(["country", "year"])
    .agg(
        mean_delay=("delai_creation_to_first_mel", "mean"),
        median_delay=("delai_creation_to_first_mel", "median")
    )
    .reset_index()
)
