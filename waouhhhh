df_dealer = df[(df["country"] == c) & (df["id_apporteur"] == dealer)].copy()

df_dealer = df_dealer.drop_duplicates(subset=["country","id_contract"])
df_dealer = df_dealer.sort_values("dte_mel")

df_dealer[["id_contract","dte_mel","dte_fin_contrat","status","mtd_total_lot"]].head(50)
