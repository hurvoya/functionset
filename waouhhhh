FUTURE_DATE = pd.Timestamp("2100-01-01")

df["dte_fin_contrat_effective"] = np.where(
    df["status"] == "INS",
    FUTURE_DATE,
    df["dte_fin_contrat"]
)

df["dte_fin_contrat_effective"] = pd.to_datetime(
    df["dte_fin_contrat_effective"],
    errors="coerce"
).dt.normalize()
