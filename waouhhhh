import matplotlib.pyplot as plt
import pandas as pd

def add_year_month_cols(df, date_col, ym_col="year_month"):
    out = df.copy()
    out[ym_col] = out[date_col].dt.to_period("M").astype(str)
    out["year"] = out[date_col].dt.year
    return out
