def month_end(s: pd.Series) -> pd.Series:
    s = pd.to_datetime(s, errors="coerce")
    s = s.dt.normalize()
    # fin de mois à 00:00:00
    return s.dt.to_period("M").dt.to_timestamp("M").dt.normalize()
